---
description: Commit the discussed changes with a message that satisfies the commit rules
argument-hint: [optional scope hint]
---

Commit the changes discussed in this conversation. Treat `$ARGUMENTS` as a scope hint when present.

This command owns no rules of its own. Follow `dev/workflows/commands/commit.md`: it requires re-reading `dev/agent_rules/git_operations.md` and the foundation commit-msg workflow before composing the message, regardless of what was read earlier in the session.
